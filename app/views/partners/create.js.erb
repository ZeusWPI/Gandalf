<% if @partner.errors.any? %>
  $('#partner-errors').html("<%= escape_javascript form_errors(@partner) %>");
<% else %>
  $('#partner-errors').html('');
  $('#new_partner')[0].reset();

  $('#table-partners tr:last').before('<%= escape_javascript render @partner %>')
<% end %>
