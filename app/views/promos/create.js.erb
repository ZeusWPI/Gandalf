<% if @promo.errors.any? %>
  $('#promo-errors').html("<%= escape_javascript form_errors(@promo) %>")
<% else %>
  $('#promo-errors').html('')
  $('#new_promo')[0].reset()
  $('#new_promo')[0].reset()
  var select = $(id).parent().find('select');
  select.select2("val","");

  $('#table-promos tr:last').before('<%= escape_javascript render @promo %>')
<% end %>
