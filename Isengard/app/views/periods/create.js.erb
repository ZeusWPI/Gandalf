$("<%= escape_javascript render @period %>").appendTo("#table-periods");
$("#new_period")[0].reset();
$("#new_registration").replaceWith("<%= escape_javascript render partial: "registrations/form" %>");
