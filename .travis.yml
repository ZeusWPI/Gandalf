language: ruby
rvm:
  - 2.0.0-p247

before_install:
  - "cd Isengard"

before_script:
  - "RAILS_ENV=test bundle exec rake db:setup"

notifications:
  email:
    recipients:
      - gandalf@zeus.ugent.be
  irc: 
    channels:
      - "wina.ugent.be#hydra"
    on_success: change
    on_failure: always


